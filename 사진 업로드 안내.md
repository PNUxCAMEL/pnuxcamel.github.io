# 갤러리 사진을 GitHub로 옮기기 (한 번만 하는 작업)

노션에서 내보낸 폴더 안에는 앨범별로 사진 폴더가 12개 있습니다.
(`Gallery/제목 없음 xxxx/` 안에 `.md` 파일과 나란히 있는, 앨범 제목 이름의 폴더들)

## 1. 폴더 이름 12개 바꾸기 (우클릭 → 이름 바꾸기)

| 현재 폴더 이름 | 새 이름 | 사진 수 |
|---|---|---|
| 뜨거운 열정으로 연구하는 우리 | `01-passion` | 91 |
| 실험에서 세상으로 | `02-field` | 24 |
| 해외 학회! | `03-conference` | 36 |
| Congratulations 🎉 | `04-congrats` | 23 |
| 로봇과 함께하는 미래 | `05-future` | 30 |
| 단합력 좋은 CAMEL | `06-team` | 29 |
| 연구실 소풍의 날~ | `07-picnic` | 37 |
| 먹고 마시고 | `08-eat` | 16 |
| 스키도 잘타지요~ | `09-ski` | 9 |
| We wish you a merry christmas 🎅 | `10-christmas` | 5 |
| 즐거운 연구실 생활 | `11-lab-life` | 28 |
| 우리는 한발 더 나아간다 | `12-graduation` | 20 |

폴더 **안의 사진 파일 이름은 바꾸지 마세요.** 그대로 써야 합니다.

예외 하나: `뜨거운 열정으로 연구하는 우리.jpg` 라는 사진 파일이 폴더 밖(.md 파일 옆)에 하나 있습니다.
이 파일을 `01-passion` 폴더 안으로 옮겨 주세요.

## 2. 사진 크기 줄이기

12개 폴더를 모두 선택 → 우클릭 → PowerToys **그림 크기 조정** → "크게(1920)" →
**"원본 그림 크기 조정(복사본 만들지 않음)"** 체크 → 조정.
끝나면 전체 용량이 100MB 안쪽으로 줄어듭니다. (GitHub는 파일 하나 25MB 제한)

## 3. GitHub에 올리기

1. `camel-lab-images` 저장소 → **Add file → Create new file**
   파일 이름 칸에 `gallery/README.md` 라고 입력 (슬래시를 치면 폴더가 생깁니다)
   내용에 아무 글자 하나 적고 → Commit changes
2. 방금 생긴 `gallery` 폴더를 클릭해서 들어간 뒤 → **Add file → Upload files**
3. 폴더를 **2~3개씩** 드래그 → Commit changes. (한 번에 파일 100개 제한이라 나눠서 올립니다)
   예) `01-passion` 혼자 / `02-field`+`03-conference`+`04-congrats` / `05`+`06`+`07` / `08`~`12`
4. 다 올린 뒤 `gallery/01-passion/` 폴더에 들어가 사진이 보이는지 확인

## 4. 홈페이지 파일 업데이트

`pnuxcamel.github.io` 저장소 → Add file → Upload files → 새 사이트 zip을 푼 폴더의 **내용물**을 전부 드래그 → Commit changes.
같은 이름 파일은 덮어씌워집니다. 1~2분 뒤 https://pnuxcamel.github.io/gallery.html 에서 확인.
